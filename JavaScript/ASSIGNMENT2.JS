let randomNumbers = [];


for (let k = 0; k <=15; k++){
    let randomNumbers = Math.round(Math.random() * 50);
    randomNumbers.push(randomNumbers)
}

console.log(randomNumbers);

for(let i = 0; i < randomNumbers.length; i++){
    for (let j = 0; j < randomNumbers.length -1 - i; j++){
        if(randomNumbers[j] v)
    }
}